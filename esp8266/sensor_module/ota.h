#ifndef OTA_H
#define OTA_H

#include <Arduino.h>

// Проверяет наличие обновления на сервере и обновляет прошивку если нужно.
// Возвращает true если обновление выполнено (ESP перезагрузится сама).
// Возвращает false если обновление не нужно или произошла ошибка.
bool ota_check_and_update();

#endif
